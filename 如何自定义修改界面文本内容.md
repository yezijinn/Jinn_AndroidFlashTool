# 如何自定义修改界面文本内容

> 适用版本: XiaoMi Flash Tool v2.1  
> 核心文件: `flash_tool.py`  
> 修改后需重新编译才生效（运行 `编译.py`）

---

## 目录

1. [软件标题 & 版本号](#1-软件标题--版本号)
2. [作者信息 & 快捷命令参考](#2-作者信息--快捷命令参考)
3. [标签页名称](#3-标签页名称)
4. [标题栏 & 状态栏](#4-标题栏--状态栏)
5. [设备信息页](#5-设备信息页)
6. [刷入面具页](#6-刷入面具页)
7. [刷Recovery页](#7-刷recovery页)
8. [文件管理页](#8-文件管理页)
9. [高级功能页](#9-高级功能页)
10. [实用工具页](#10-实用工具页)
11. [投屏控制页](#11-投屏控制页)
12. [二维码区域](#12-二维码区域)
13. [命令输出 & 提示框](#13-命令输出--提示框)
14. [修改完成后](#14-修改完成后)

---

## 1. 软件标题 & 版本号

**文件位置**: `flash_tool.py`，约第 112 行

```python
self.root.title("小米刷机助手 v2.1")     # ← 窗口标题（任务栏显示）
```

**文件位置**: 约第 174-175 行

```python
tk.Label(hdr, text="  小米刷机助手", ...)   # ← 标题栏主标题
tk.Label(hdr, text="v2.1", ...)            # ← 标题栏版本号
```

**示例 - 修改为自定义名称**:

```python
self.root.title("我的刷机工具 v1.0")
tk.Label(hdr, text="  我的刷机工具", ...)
tk.Label(hdr, text="v1.0", ...)
```

---

## 2. 作者信息 & 快捷命令参考

**文件位置**: `flash_tool.py`，约第 565-601 行，`_open_quick_cmd()` 方法

整个字符串内容就是快捷命令参考窗口显示的文本，可直接修改：

```python
def _open_quick_cmd(self):
    c="""B站：叶子Jinn                # ← 修改这里自定义作者信息
B站、淘宝店铺同名：叶子Jinn

fastboot oem device-info          :解BL锁状态（高通骁龙）
fastboot oem lks                  :解BL锁状态（联发科天玑）
-----------------------------------------
...（其余命令列表）...
```

另外该窗口的标题在第 602 行：

```python
win.title("快捷命令参考")           # ← 窗口标题
tk.Label(win, text="常用命令参考", ...)  # ← 窗口内标题
```

**示例**:

```python
c="""作者：张三
微信公众号：张三科技

fastboot oem device-info          :查看BL锁状态
..."""
```

---

## 3. 标签页名称

**文件位置**: `flash_tool.py`

每个标签页在 `nb.add()` 时通过 `text=` 设置名称，共 7 个标签页：

| 标签页 | 所在方法 | 行号 | 当前文本 |
|--------|---------|------|---------|
| 设备信息 | `_tab_device` | 368 | `"  设备信息  "` |
| 刷入面具 | `_tab_magisk` | 399 | `"  刷入面具  "` |
| 刷Recovery | `_tab_recovery` | 441 | `"  刷Recovery  "` |
| 文件管理 | `_tab_files` | 476 | `"  文件管理  "` |
| 高级功能 | `_tab_advanced` | 535 | `"  高级功能  "` |
| 实用工具 | `_tab_tools` | 635 | `"  实用工具  "` |
| 投屏控制 | `_tab_screen` | 654 | `"  投屏控制  "` |

**修改方法**: 找到对应行，修改 `text=` 参数即可。

> 注意: 文本首尾的空格用于增加标签页间距，建议保留。

---

## 4. 标题栏 & 状态栏

**文件位置**: `flash_tool.py`，`_build_ui()` 方法

| 元素 | 行号 | 当前文本 | 说明 |
|------|------|---------|------|
| 管理员文字 | 178 | `"管理员" / "非管理员"` | 运行时自动判断 |
| 管理员圆点 | 179 | `"●"` | 状态圆点符号 |
| 命令输出标题 | 197 | `"命令输出"` | 输出区标题 |
| 终止命令按钮 | 199 | `"终止命令"` | 红色按钮 |
| 清空按钮 | 200 | `"清空"` | 清空输出 |
| 状态栏默认文本 | 217 | `"就绪"` | 底部状态栏 |

**示例**:

```python
self._btn(bh, "停止", self._kill_cmd, 'danger', width=80, height=30)  # 改按钮文字
self.status = tk.Label(sbar, text="准备就绪", ...)                     # 改状态栏
```

---

## 5. 设备信息页

**文件位置**: `flash_tool.py`，`_tab_device()` 方法（约 367-393 行）

### LabelFrame 标题

| 行号 | 当前文本 |
|------|---------|
| 377 | `"ADB 设备信息 (手机需开启USB调试)"` |
| 382 | `"Fastboot 设备信息 (手机需在Fastboot模式)"` |
| 390 | `"重启操作"` |

### 按钮文字

| 分组 | 按钮文字 | 行号 |
|------|---------|------|
| ADB | `"检测ADB设备"`, `"查看机型"`, `"查看内核"` | 379 |
| Fastboot | `"检测设备"`, `"查看机型"`, `"查看Slot"` | 384 |
| BL锁 | `"BL锁(骁龙)"`, `"BL锁(天玑)"` | 387 |
| 重启 | `"重启到Fastboot"`, `"重启到Recovery"`, `"重启系统"` | 392 |

**示例 - 修改按钮**:

```python
# 原: 379行
for t,x,a in [("检测ADB设备","adb",["devices"]), ...]

# 改:
for t,x,a in [("检查设备","adb",["devices"]), ...]
```

---

## 6. 刷入面具页

**文件位置**: `flash_tool.py`，`_tab_magisk()` 方法（约 398-420 行）

| 类型 | 当前文本 | 行号 |
|------|---------|------|
| 说明文字 | `"将Magisk/KernelSU镜像刷入boot分区以获取ROOT权限"` | 400 |
| 标签 | `"镜像文件:"` | 402 |
| 按钮 | `"浏览..."` | 404 |
| LabelFrame | `"刷入方式"` | 405 |
| 单选项 | `"flash boot (单分区)"`, `"flash init_boot (单分区)"`, `"flash init_boot_ab (A/B分区)"` | 407 |
| 按钮 | `"开始刷入"` | 410 |
| Section标题 | `"从手机拉取boot镜像"` | 412 |
| 标签 | `"手机路径:"` | 414 |
| 输入框默认值 | `/storage/emulated/0/Download/2.img` | 415 |
| 标签 | `"保存到:"` | 417 |
| 按钮 | `"浏览..."` | 419 |
| 按钮 | `"拉取"` | 420 |

---

## 7. 刷Recovery页

**文件位置**: `flash_tool.py`，`_tab_recovery()` 方法（约 440-453 行）

| 类型 | 当前文本 | 行号 |
|------|---------|------|
| 说明文字 | `"刷入Recovery镜像 (TWRP等)"` | 442 |
| 标签 | `"Recovery镜像:"` | 444 |
| 按钮 | `"浏览..."` | 446 |
| LabelFrame | `"刷入方式"` | 447 |
| 单选项 | `"仅刷recovery (单分区)"`, `"刷recovery_a + recovery_b (A/B)"`, `"刷misc.bin + recovery (推荐)"`, `"临时启动 (不写入)"` | 449 |
| 按钮 | `"开始刷入"` | 452 |
| 按钮 | `"重启进入Recovery"` | 453 |

---

## 8. 文件管理页

**文件位置**: `flash_tool.py`，`_tab_files()` 方法（约 475-503 行）

| 类型 | 当前文本 | 行号 |
|------|---------|------|
| LabelFrame | `"安装APK"` | 477 |
| 标签 | `"APK:"` | 479 |
| 按钮 | `"浏览..."`, `"安装"` | 481-482 |
| LabelFrame | `"推送到手机 (adb push)"` | 484 |
| 标签 | `"电脑文件/文件夹:"` | 486 |
| 按钮 | `"文件"`, `"文件夹"` | 488-489 |
| 标签 | `"手机路径:"` | 491 |
| 输入框默认值 | `/storage/emulated/0/` | 492 |
| 按钮 | `"推送"` | 493 |
| LabelFrame | `"从手机拉取文件 (adb pull)"` | 495 |
| 标签 | `"手机路径:"` | 497 |
| 输入框默认值 | `/storage/emulated/0/Download/` | 498 |
| 标签 | `"保存到:"` | 500 |
| 按钮 | `"浏览..."`, `"拉取"` | 502-503 |

---

## 9. 高级功能页

**文件位置**: `flash_tool.py`，`_tab_advanced()` 方法（约 534-551 行）+ `_open_quick_cmd()`（约 565-607 行）

### 按钮文字

| 按钮 | 行号 |
|------|------|
| `"BL解锁"` | 537 |
| `"修复hosts加速下载"` | 538 |
| `"修复USB驱动注册表"` | 539 |
| `"刷新DNS"` | 540 |
| `"浏览..."` | 544 |
| `"刷入(禁用验证)"` | 545 |
| `"执行"` | 549 |
| `"快捷命令参考"` | 551 |

### 标签

| 标签 | 行号 |
|------|------|
| `"vbmeta:"` | 542 |
| `"自定义命令:"` | 547 |

### 快捷命令参考窗口

```python
win.title("快捷命令参考")    # 窗口标题（第602行）
tk.Label(win, text="常用命令参考", ...)  # 窗口内标题（第604行）
self._btn(win, "关闭", ...)  # 关闭按钮（第607行）
```

---

## 10. 实用工具页

**文件位置**: `flash_tool.py`，`_tab_tools()` 方法（约 634-643 行）

| 按钮 | 行号 |
|------|------|
| `"打开设备管理器"` | 637 |
| `"安装OPPO驱动"` | 638 |
| `"内存清理 (Cleaner)"` | 639 |
| `"安装压缩工具 (Bandizip)"` | 641 |
| `"关闭UA警告 (bat)"` | 642 |
| `"关闭UA警告 (ps1)"` | 643 |

---

## 11. 投屏控制页

**文件位置**: `flash_tool.py`，`_tab_screen()` 方法（约 653-687 行）

| 类型 | 当前文本 | 行号 |
|------|---------|------|
| 按钮 | `"开始投屏"` | 659 |
| 按钮 | `"结束投屏"` | 660 |
| 按钮 | `"结束ADB进程"` | 661 |
| 勾选框 | `"自动息屏 (投屏时关闭手机屏幕)"` | 665 |
| Section标题 | `"投屏参数:"` | 668 |
| 标签 | `"码率:"` | 671 |
| 标签 | `"(默认8M, 越高越清晰)"` | 673 |
| 标签 | `"分辨率上限:"` | 675 |
| 标签 | `"(0=原尺寸)"` | 677 |
| Section标题 | `"手机导航键 (鼠标点击):"` | 679 |
| 导航按钮 | `"← 返回"`, `"■ 主页"`, `"□ 后台"` | 681 |
| Section标题 | `"其他快捷操作:"` | 684 |
| 快捷按钮 | `"电源"`, `"音量+"`, `"音量-"`, `"截图"`, `"展开通知栏"` | 686-687 |

---

## 12. 二维码区域

**文件位置**: `flash_tool.py`，`_build_qrcode()` 方法（约 222-248 行）

```python
# 每张图片的下标文字
items = [
    ("_wxpay_thumb.png",  "微信收款",    "微信收款.png"),    # ← 标签文字
    ("_alipay_thumb.png", "支付宝收款",  "支付宝收款.png"),  # ← 标签文字
    ("_wxfriend_thumb.png","微信好友",   "微信好友.png"),     # ← 标签文字
    ("_qqfriend_thumb.png","QQ好友",     "QQ好友.png"),       # ← 标签文字
]
# "大图"按钮（第246行）
self._btn(bf, "大图", ...)    # ← 按钮文字
```

**示例**:

```python
items = [
    ("_wxpay_thumb.png",  "微信扫码",   "微信收款.png"),
    ("_alipay_thumb.png", "支付宝扫码", "支付宝收款.png"),
    ("_wxfriend_thumb.png","加微信",    "微信好友.png"),
    ("_qqfriend_thumb.png","加QQ",      "QQ好友.png"),
]
```

### 查看大图窗口

| 文本 | 行号 |
|------|------|
| `"提示"`、`"图片未找到"` | 251 |
| `"查看大图 - "` | 252 |
| `"关闭"` | 262 |
| `"错误"` | 266 |

---

## 13. 命令输出 & 提示框

### 日志 TAG

在 `_log()` 输出中使用，约第 208 行：

```python
for tag,clr in [('cmd',C['blue']),('ok',C['green']),('warn',C['yellow']),('err',C['red']),('info',C['text'])]:
```

这些 tag 控制输出文字颜色，一般无需修改。

### 信息提示框

分布在多个方法中，都使用 `messagebox.showwarning()` / `messagebox.showerror()`：

| 提示文本 | 所在方法 | 行号 |
|---------|---------|------|
| `"请先选择有效的镜像文件"` | `_do_flash_magisk` | 430 |
| `"请填写保存路径"` | `_do_pull_boot` | 434 |
| `"请先选择Recovery镜像"` | `_do_flash_recov` | 460 |
| `"请选择APK文件"` | `_do_install_apk` | 519 |
| `"请选择源文件"` | `_do_push` | 523 |
| `"请填写手机路径"` | `_do_push` | 524 |
| `"请填写路径"` | `_do_pull` | 528 |
| `"需要管理员权限!"` | `_fix_hosts` / `_fix_usb` | 610 / 620 |
| `"请选择vbmeta镜像"` | `_do_vbmeta` | 558 |
| `"scrcpy.exe 未找到"` | `_start_scrcpy` | 715 |
| `"未检测到ADB设备，请确保手机已连接并开启USB调试"` | `_start_scrcpy` | 720 |

---

## 14. 修改完成后

1. **保存** `flash_tool.py`
2. **运行** `编译.py` 重新打包生成 exe
3. 新 exe 生成在 `dist/XiaoMiFlashTool.exe`

> 提示: 每次修改代码后，务必删除旧的 `build/` 目录和 `XiaoMiFlashTool.spec` 文件再编译，否则更改可能不会生效。`编译.py` 会自动处理这些清理步骤。