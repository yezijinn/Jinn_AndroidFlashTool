# 小米刷机助手 - 项目开发手册

> 本文档详细记录了「小米刷机助手」从零到一的完整开发过程，任何人可根据此文档复现整个项目。

---

## 一、项目概述

| 项目 | 说明 |
|------|------|
| 名称 | 小米刷机助手 (XiaoMi Flash Tool) |
| 版本 | v2.1 |
| 类型 | Windows 桌面 GUI 工具 |
| 用途 | 安卓手机刷机、ROOT、Recovery、文件管理、投屏控制 |
| 目标系统 | Windows 7/8/10/11 64位 |
| 交付形式 | 单文件 exe (42.88MB) |
| 外部依赖 | 零（无需安装Python/.NET/任何运行时） |

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 编程语言 | Python 3.11 | 系统自带标准库足够 |
| GUI框架 | tkinter (ttk + Canvas) | 标准库，无需pip安装 |
| 图片处理 | 无（移除PIL，用原生tk.PhotoImage） | 预生成PNG缩略图 |
| 打包工具 | PyInstaller 6.21 | `--onefile --windowed` 单文件 |
| 核心工具 | ADB + Fastboot + scrcpy | 安卓调试三件套 |
| UI组件 | 自定义RoundedButton (Canvas绘制) | 圆角+阴影+悬停+按压 |

### 为什么选Python而不是C？
- tkinter是标准库，Win7-11自带Tcl/Tk运行时
- 开发效率高，单文件打包简单
- ADB/Fastboot本身就是命令行工具，Python只做GUI壳
- C需要手动管理窗口、消息循环、控件绘制，开发周期10倍以上

---

## 三、项目结构

```
flash_tool/
├── flash_tool.py              # 主程序源码 (~700行)
├── 项目开发明细.md            # 本文档
├── 问答日志.md                # 用户提问记录
├── 开发日志.md                # 开发步骤记录
├── tools/                     # 核心工具 (打包进exe, ~17MB)
│   ├── adb.exe               # ADB调试工具
│   ├── fastboot.exe          # Fastboot刷机工具
│   ├── scrcpy.exe            # 投屏工具
│   ├── scrcpy-server         # scrcpy服务端
│   ├── AdbWinApi.dll         # ADB依赖
│   ├── AdbWinUsbApi.dll      # ADB依赖
│   ├── SDL2.dll              # scrcpy依赖
│   ├── avcodec-61.dll        # scrcpy依赖
│   ├── avformat-61.dll       # scrcpy依赖
│   ├── avutil-59.dll         # scrcpy依赖
│   ├── swresample-5.dll      # scrcpy依赖
│   ├── libusb-1.0.dll        # scrcpy依赖
│   └── misc.bin              # Recovery刷入用misc镜像
├── XiaoMi/                    # 资源目录 (部分打包进exe)
│   ├── _微信.支付宝.经营收款码.png   # 收款码原图 (打包)
│   ├── _qrcode_thumb.png             # 收款码缩略图280x155 (打包)
│   ├── +微信好友.png                 # 微信好友码原图 (打包)
│   ├── _wechat_thumb.png             # 微信缩略图 (打包)
│   ├── +QQ好友.png                   # QQ好友码 (JPG转PNG, 打包)
│   ├── _qq_thumb.png                 # QQ缩略图 (打包)
│   ├── OPPO.exe                      # OPPO驱动安装 (打包, 14MB)
│   ├── WinMemoryCleaner.exe          # 内存清理工具 (打包, 0.5MB)
│   ├── Bandizipv6.29.exe             # Bandizip压缩工具 (打包, 6MB)
│   ├── DisableAllFileWarnings.bat    # 关闭UA警告BAT脚本 (打包)
│   └── DisableAllFileWarnings.ps1    # 关闭UA警告PS1脚本 (打包)
└── dist/
    └── XiaoMiFlashTool.exe    # 最终编译产物 (42.88MB)
```

---

## 四、核心代码架构

### 4.1 整体结构
```
flash_tool.py
├── 路径常量与工具函数 (APP_DIR, resource_path, xiaomi_path, get_tool_path)
├── 颜色主题字典 C (21种颜色)
├── RoundedButton类 (Canvas圆角按钮组件)
└── App类 (主程序)
    ├── __init__()          # 初始化
    ├── _btn()              # 按钮工厂方法 (6种样式)
    ├── _setup_styles()     # ttk.Style配置
    ├── _build_ui()         # 主界面构建
    ├── _build_qrcode()     # 二维码区域
    ├── _view_large()       # 查看大图弹窗
    ├── _log()              # 日志输出
    ├── _terminate()        # 通用进程终止
    ├── _exec_subprocess()  # 通用子进程执行 (核心)
    ├── _run()              # 命令队列执行
    ├── _run_file()         # 本地文件执行
    ├── _browse()           # 统一文件对话框
    ├── _tab_device()       # Tab1: 设备信息
    ├── _tab_magisk()       # Tab2: 刷入面具
    ├── _tab_recovery()     # Tab3: 刷Recovery
    ├── _tab_files()        # Tab4: 文件管理
    ├── _tab_advanced()     # Tab5: 高级功能
    ├── _tab_tools()        # Tab6: 实用工具
    ├── _tab_screen()       # Tab7: 投屏控制
    └── run()               # 启动主循环
```

### 4.2 RoundedButton 圆角按钮
```python
class RoundedButton(tk.Canvas):
    """Canvas绘制圆角按钮: 阴影+悬停变色+按压反馈"""
    # 圆角矩形: create_polygon(smooth=True) 12点近似圆角
    # 阴影: 底部偏移4px的深色圆角矩形
    # 悬停: <Enter>事件重绘为hover_bg颜色
    # 按压: <Button-1>重绘为active_bg颜色, <ButtonRelease-1>执行command
```

6种按钮样式：
| 样式 | 背景色 | 用途 |
|------|--------|------|
| default | #1c1c35 (暗紫) | 普通按钮 |
| primary | #6366f1 (蓝) | 主要操作(刷入/执行) |
| danger | #ef4444 (红) | 危险操作(BL解锁/终止) |
| green | #10b981 (绿) | 安装/开始投屏 |
| cyan | #06b6d4 (青) | 备用 |
| outline | #141425 (轮廓) | 次要操作 |

### 4.3 通用子进程执行
```python
def _exec_subprocess(self, cmd, label, on_done=None, proc_attr='current_process'):
    """统一处理: Popen + readline实时输出 + after线程安全 + 状态管理"""
    # proc_attr参数支持 'current_process'(普通命令) 或 'scrpcy_process'(投屏)
```

### 4.4 命令队列
```python
def _run(self, tool, args):
    """fastboot是阻塞式(无设备时一直等待), 用队列顺序执行避免UI卡死"""
    self._cmd_queue.append((tool, args))
    if not self.is_running: self._run_next()
```

---

## 五、功能清单

### Tab 1: 设备信息
- ADB: 检测设备、查看机型、查看内核
- Fastboot: 检测设备、查看机型、查看Slot、BL锁状态(骁龙/天玑)
- 重启: 到Fastboot、到Recovery、重启系统

### Tab 2: 刷入面具 (Magisk/KernelSU)
- 选择镜像文件
- 刷入方式: flash boot / flash init_boot / flash init_boot_ab
- 从手机拉取boot镜像

### Tab 3: 刷Recovery
- 选择Recovery镜像
- 刷入方式: 仅recovery / recovery_a+b / misc+recovery / 临时启动
- 重启进入Recovery

### Tab 4: 文件管理
- 安装APK
- 推送文件/文件夹到手机 (adb push)
- 从手机拉取文件 (adb pull)

### Tab 5: 高级功能
- BL解锁
- 修复hosts加速下载
- 修复USB驱动注册表
- 刷新DNS
- 刷入vbmeta(禁用验证)
- 自定义命令执行
- 快捷命令参考

### Tab 6: 实用工具
- 打开设备管理器
- 安装OPPO驱动
- 内存清理 (WinMemoryCleaner)
- 安装压缩工具 (Bandizip)
- 关闭UA警告 (bat/ps1)

### Tab 7: 投屏控制 (scrcpy)
- 开始/结束投屏 (始终亮屏+关屏+无音频)
- 结束ADB进程
- 码率/分辨率参数设置
- 手机导航键: 返回/主页/后台
- 快捷键: 电源/音量+/音量-/截图/通知栏

### 底部区域
- 左: 命令输出窗口 (Consolas等宽字体, 5色标签)
- 右: 二维码 (收款码+微信好友+QQ好友, 带查看大图)

---

## 六、打包命令

### 环境准备
```powershell
# Python 3.11 + PyInstaller
pip install pyinstaller
```

### 图片预处理 (一次性, 生成缩略图)
```python
from PIL import Image
# JPG转PNG
Image.open('+QQ好友.jpg').save('+QQ好友.png')
# 生成缩略图
Image.open('_微信.支付宝.经营收款码.png').resize((280,155), Image.LANCZOS).save('_qrcode_thumb.png')
# ... 微信/QQ缩略图同理
```

### 编译命令
```powershell
python -m PyInstaller --onefile --windowed --noupx --strip ^
  --name XiaoMiFlashTool ^
  --add-data "tools;tools" ^
  --add-data "XiaoMi\_qrcode_thumb.png;XiaoMi" ^
  --add-data "XiaoMi\_wechat_thumb.png;XiaoMi" ^
  --add-data "XiaoMi\_qq_thumb.png;XiaoMi" ^
  --add-data "XiaoMi\_微信.支付宝.经营收款码.png;XiaoMi" ^
  --add-data "XiaoMi\+微信好友.png;XiaoMi" ^
  --add-data "XiaoMi\+QQ好友.png;XiaoMi" ^
  --add-data "XiaoMi\DisableAllFileWarnings.bat;XiaoMi" ^
  --add-data "XiaoMi\DisableAllFileWarnings.ps1;XiaoMi" ^
  --add-data "XiaoMi\OPPO.exe;XiaoMi" ^
  --add-data "XiaoMi\WinMemoryCleaner.exe;XiaoMi" ^
  --add-data "XiaoMi\Bandizipv6.29.exe;XiaoMi" ^
  --exclude-module PIL --exclude-module Pillow ^
  --exclude-module unittest --exclude-module email ^
  --exclude-module xml --exclude-module pydoc ^
  flash_tool.py
```

### 体积优化要点
| 优化项 | 节省 |
|--------|------|
| 移除PIL/Pillow | -11MB |
| 不打包MiFlash2018.zip(50MB) | -50MB |
| 精确打包(只打包必要文件) | -35MB |
| `--exclude-module` 排除不用的标准库 | -2MB |
| **最终体积** | **42.88MB** |

### 踩坑记录
1. **不能排除urllib**: pathlib依赖urllib，`--exclude-module urllib`会导致启动报错 `ModuleNotFoundError: No module named 'urllib'`
2. **APP_DIR路径**: PyInstaller打包后 `__file__` 指向临时目录，需用 `sys.executable` 获取真实路径
3. **中文乱码**: 用 `locale.getpreferredencoding()` 获取系统编码(GBK)，不用硬编码utf-8
4. **fastboot阻塞**: fastboot无设备时会一直等待，需用命令队列+终止按钮

---

## 七、颜色主题

```python
C = {
    'bg':'#0b0b14',       # 主背景 (极深紫黑)
    'surface':'#141425',  # 表面层 (标题栏/状态栏)
    'card':'#1c1c35',     # 卡片层 (按钮/输出区)
    'overlay':'#262640',  # 悬浮层 (悬停色)
    'text':'#edf2f7',     # 主文字 (近白)
    'subtext':'#8892b0',  # 次要文字 (灰蓝)
    'blue':'#6366f1',     # 强调蓝 (主操作)
    'green':'#10b981',    # 成功绿
    'red':'#ef4444',      # 危险红
    'yellow':'#f59e0b',   # 标题黄
    'dark':'#08080f',     # 输出区背景 (极深)
    'border':'#2a2a45',   # 边框
    'shadow':'#040408',   # 按钮阴影
}
```

---

## 八、AI提示词（优化版）

以下是开发过程中使用的优化版提示词，可直接用于AI复现项目：

### 提示词1: 项目创建
```
制作Windows刷机工具exe程序。
要求: 单文件exe, Win7-11 64位, 零外部依赖(不装Python/.NET)。
功能: 基于adb/fastboot命令, 现代GUI(tkinter), 按钮交互, 实时命令输出窗口。
技术: Python 3.11 + tkinter + PyInstaller(--onefile --windowed)。
编码: locale.getpreferredencoding()适配中文Windows(GBK)。
路径: hasattr(sys,'_MEIPASS')判断打包模式, APP_DIR用sys.executable。
```

### 提示词2: UI美化
```
UI美化8项需求:
1. Canvas自定义RoundedButton替代ttk.Button (圆角10px+阴影4px+悬停变色+按压反馈)
2. 6种按钮样式: default/primary/danger/green/cyan/outline
3. 布局: padx=18 pady=4-10, 不拥挤
4. 字体: 标题18px bold, 输出区Consolas 10px等宽
5. 暗色主题: #0b0b14背景/#1c1c35卡片/#6366f1强调蓝
6. 标签页: 选中蓝字+悬停高亮+22px padding
7. 输入框: 焦点蓝色边框+深色背景
8. 标题栏: 底部蓝色光晕线+管理员圆点指示器
```

### 提示词3: 体积优化
```
exe体积<50MB。
方案: 精确打包(只打包必要文件), 大文件外部读取。
打包进exe: tools/(17MB) + 图片缩略图(1.3MB) + 驱动/清理/压缩工具(20MB)
不打包: MiFlash2018.zip(50MB不需要)
排除: PIL, unittest, email, xml, pydoc (不能排除urllib, pathlib依赖它)
移除PIL: 预生成PNG缩略图, 用tk.PhotoImage原生加载
```

### 提示词4: 功能增强
```
新增功能:
1. scrcpy投屏: --stay-awake --turn-screen-off --no-audio, 码率/分辨率可调
2. 手机导航键: 返回(4)/主页(3)/后台(187) via adb shell input keyevent
3. 二维码: 收款码280x155+好友码130px, 查看大图弹窗(70%屏幕)
4. 实用工具: 设备管理器/OPPO驱动/内存清理/Bandizip/关闭UA警告
5. 命令队列: fastboot阻塞式, 用队列顺序执行+终止按钮
6. 高级: BL解锁/hosts修复/USB注册表修复/vbmeta刷入/自定义命令
```

### 提示词5: 代码优化
```
代码审查优化:
1. 提取_exec_subprocess(cmd,label,proc_attr)通用方法, 消除3处重复的Popen+readline+after模式
2. 合并5个_browse*方法为1个_browse(title,mode,ext)
3. 提取_terminate(p,label)通用进程终止方法
4. _exec_subprocess用proc_attr参数支持current_process和scrpcy_process两种引用
```

---

## 九、开发历程

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| v1.0 | 2026-06-20 | 基础tkinter GUI, Catppuccin主题 |
| v1.1 | 2026-06-20 | 代码审查修复(命令队列/路径/编码) |
| v1.2 | 2026-06-20 | UI布局优化(侧边栏/输出窗口/文件对话框) |
| v1.3 | 2026-06-20 | 二维码+实用工具Tab+UA警告 |
| v2.0 | 2026-06-20 | scrcpy投屏+UI美化 |
| v2.1 | 2026-06-21 | RoundedButton圆角按钮+体积瘦身42MB+代码重构 |

---

## 十、复现步骤

1. **安装Python 3.11** (勾选Add to PATH)
2. **安装PyInstaller**: `pip install pyinstaller`
3. **下载工具**: adb.exe, fastboot.exe, scrcpy.exe及DLL放入 `tools/`
4. **准备资源**: 二维码图片、OPPO.exe、WinMemoryCleaner.exe、Bandizipv6.29.exe放入 `XiaoMi/`
5. **生成缩略图**: 用PIL一次性生成PNG缩略图(280x155, 130px)
6. **编写代码**: 按4.x节架构编写flash_tool.py
7. **编译**: 按第六节命令执行PyInstaller
8. **验证**: dist/XiaoMiFlashTool.exe < 50MB, 双击运行

---

## 十一、注意事项

1. **管理员权限**: hosts修复/USB注册表修复需要管理员权限运行
2. **USB调试**: 手机需开启USB调试模式才能使用ADB功能
3. **Fastboot模式**: 手机需进入Fastboot模式(音量下+电源键)才能刷机
4. **编码问题**: 中文Windows默认GBK编码, 不要硬编码utf-8
5. **路径问题**: PyInstaller打包后 `__file__` 指向临时目录, 必须用 `sys.executable`
6. **单文件限制**: exe+XiaoMi文件夹需放在同一目录, 大文件从外部读取
