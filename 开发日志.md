---

## 2026-06-24 防篡改#28: UPX加壳+运行时完整性自校验

**需求**: UPX压缩exe + 启动时检测自身是否被篡改

**实现**:
1. 下载UPX 4.0.2 win64至upx\upx-4.0.2-win64\upx.exe
2. flash_tool.py: 新增_verify_integrity() → 读取exe末尾44字节(SHA256 Base64)比对
3. 编译.py: 步骤1清理→2PyInstaller→3UPX --best加壳→4追加SHA256校验码→5清理

**流程**: 编译.exe → UPX压缩 → 计算SHA256 → Base64编码(44字节) → 追加到exe末尾
**启动校验**: sys.argv[0]→读文件→取末44字节→计算data[:-44]的SHA256→比对→不匹配弹窗退出

---

## 2026-07-02 修复#29: 中文路径乱码

**问题**: adb push/pull/install遇到中文路径显示乱码，导致文件传输失败

**根因**: adb命令无法正确处理Unicode中文路径

**修复**: 添加get_short_path()函数，调用Windows API GetShortPathNameW将中文长路径转为8.3短路径

**涉及**: _do_push/_do_pull/_do_pull_boot/_do_install_apk 全部添加路径转换